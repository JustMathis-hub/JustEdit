/**
 * Promo prices for the launch offer.
 * Maps product slug → sale price in cents.
 * When a slug is listed here, the original price is shown struck through.
 */
export const PROMO_PRICES: Record<string, number> = {
};
